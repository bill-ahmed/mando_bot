{
  "name": "mando_bot",
  "version": "1.0.0",
  "description": "A Slack bot :)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "set DEGUB=* && ts-node-dev --respawn --transpileOnly index.ts",
    "build": "tsc && cp config.json ./build"
  },
  "author": "Bilal Ahmed",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^1.5.0",
    "@types/request": "^2.48.4",
    "request": "^2.88.0",
    "ts-node-dev": "^1.0.0-pre.44",
    "typescript": "^3.7.4",
    "winston": "^3.2.1"
  }
}
